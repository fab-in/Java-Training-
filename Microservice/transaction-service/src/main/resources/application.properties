spring.application.name=transaction-service
server.port=8083

# Database Configuration - Transactions DB
spring.datasource.url=jdbc:mysql://localhost:3306/transactions_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.username=root
spring.datasource.password=admin123

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Second DataSource for OTP DB (if using separate DB)
# OR use same DB with different schema
# For simplicity, use same DB but different tables

# RabbitMQ Configuration
spring.rabbitmq.host=localhost
spring.rabbitmq.port=5672
spring.rabbitmq.username=guest
spring.rabbitmq.password=guest

# Wallet Service URL
wallet.service.url=http://localhost:8082

# User Service URL
user.service.url=http://localhost:8081

# Email Configuration (for OTP and statement emails)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=fabinchandi@gmail.com
spring.mail.password=ghoe cext iahb knso
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Actuator
management.endpoints.web.exposure.include=health,metrics